{
  "$schema": "./node_modules/testguard-cli/spec/schemas/claims.schema.json",
  "schemaVersion": 1,
  "claims": [
    {
      "id": "GENERATE-IO-BACKUPFILE",
      "statement": "backupFile never destroys a recoverable file: it skips the write when the target already equals what is about to be written, and never replaces a .bak DocGuard did not write.",
      "source": {
        "kind": "manual",
        "ref": "testguard scaffold cli/writers/generate-io.mjs"
      },
      "severity": "high",
      "producedBy": {
        "producer": "derived",
        "by": "testguard scaffold 0.6.0"
      },
      "defendedBy": [
        "tests/hook-fail-open.test.mjs",
        "tests/hooks-contract.test.mjs",
        "tests/generate-io.test.mjs"
      ],
      "faults": [
        {
          "id": "S4",
          "description": "[line 26] Guard removed: `if (nextContent !== null && content === nextContent) return;` no longer runs.",
          "faultClass": "statement-deleted",
          "file": "cli/writers/generate-io.mjs",
          "find": "    if (nextContent !== null && content === nextContent) return;",
          "replace": "",
          "producedBy": {
            "producer": "derived",
            "by": "testguard scaffold 0.6.0"
          }
        },
        {
          "id": "S5",
          "description": "[line 36] Guard never triggers: `if (prior !== content)` becomes `if (false)`.",
          "faultClass": "condition-forced",
          "file": "cli/writers/generate-io.mjs",
          "find": "      if (prior !== content) {",
          "replace": "      if (false) {",
          "producedBy": {
            "producer": "derived",
            "by": "testguard scaffold 0.6.0"
          }
        }
      ]
    },
    {
      "id": "BENCHMARK-BASELINE-FAIL-CLOSED",
      "statement": "A persisted benchmark baseline is quotable only when its metrics and caveat are recomputable from its retained cases: the loader rejects a hand-edited ratio, a stale caveat, a measure other than benchmark-precision, and the pre-provenance envelope, before any benchmark run starts.",
      "source": {
        "kind": "spec",
        "ref": "specs/007-precision-evidence-loop/spec.md#FR-018"
      },
      "severity": "high",
      "producedBy": {
        "producer": "human",
        "by": "Claude Fable 5.1 with maintainer review, 2026-09-18"
      },
      "defendedBy": [
        "tests/benchmark-baseline.test.mjs"
      ],
      "faults": [
        {
          "id": "B1",
          "description": "Metrics integrity check removed: a hand-edited core.metrics is accepted.",
          "faultClass": "condition-forced",
          "file": "benchmarks/lib/baseline.mjs",
          "find": "  if (JSON.stringify(core.metrics) !== JSON.stringify(recomputed)) {",
          "replace": "  if (false) {",
          "producedBy": {
            "producer": "human",
            "by": "Claude Fable 5.1 with maintainer review, 2026-09-18"
          }
        },
        {
          "id": "B2",
          "description": "Caveat check removed: a stale or edited review.caveat is accepted.",
          "faultClass": "condition-forced",
          "file": "benchmarks/lib/baseline.mjs",
          "find": "  if (review.caveat !== expectedCaveat) {",
          "replace": "  if (false) {",
          "producedBy": {
            "producer": "human",
            "by": "Claude Fable 5.1 with maintainer review, 2026-09-18"
          }
        },
        {
          "id": "B3",
          "description": "Measure check removed: review.measures may claim calibration.",
          "faultClass": "condition-forced",
          "file": "benchmarks/lib/baseline.mjs",
          "find": "  if (review.measures !== BASELINE_MEASURES) {",
          "replace": "  if (false) {",
          "producedBy": {
            "producer": "human",
            "by": "Claude Fable 5.1 with maintainer review, 2026-09-18"
          }
        },
        {
          "id": "B4",
          "description": "Legacy envelope guard removed: a schemaVersion 1 baseline without provenance is no longer refused with the regeneration message.",
          "faultClass": "statement-deleted",
          "file": "benchmarks/lib/baseline.mjs",
          "find": "  if (value.schemaVersion === 1 && value.$schema === undefined) {\n    throw new Error('Baseline envelope schemaVersion 1 predates the provenance contract (measures, caveat); '\n      + 'regenerate it with --write-baseline --replace-baseline after review.');\n  }\n",
          "replace": "",
          "producedBy": {
            "producer": "human",
            "by": "Claude Fable 5.1 with maintainer review, 2026-09-18"
          }
        }
      ]
    }
  ]
}

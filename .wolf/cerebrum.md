# Cerebrum — docguard-cli

> OpenWolf learning memory for **docguard-cli** — the enforcement tool for Canonical-Driven Development (CDD). Audits, generates, and guards project documentation.
> Last updated: 2026-06-02

## User Preferences

- **2026-07-03** — **NO VS Code extension. Ever (until Ricardo says otherwise).** He built one before, it was deeply problematic ("a shit show"), and he removed it. Do not propose, scaffold, or build a VS Code extension for DocGuard. SARIF + Problems-panel via existing SARIF viewers is the sanctioned path to editor visibility. All OTHER distribution channels approved 2026-07-03: pre-commit, MCP registries (official/Smithery/mcp.so/Glama), awesome lists, Homebrew tap, GitLab CI component, Actions Marketplace (his click). #286 catalog submission SENT as-is — sharpened description goes into release.yml sync-catalog for the NEXT release, don't touch the submitted issue.
- **2026-07-03** — Keep the LOCAL main tree (`/Users/ricardoaccioly/Repo_claude/canonical-spec-kit`) always up to date with origin/main. After any merge/push to main (releases especially), sync it in the same session: `git -C <main> status --short` first (never clobber unrecognized work — stash leftovers with a message, surface real WIP), then `git -C <main> pull --ff-only`. Sessions run in auto-worktrees, so the main tree silently falls behind otherwise (it sat at v0.25 while origin hit v0.29).

- On field/bug reports, Ricardo prefers **"triage & verify first"** over jumping to fixes — confirm each item against current code (reproduce where possible) and explicitly challenge the report's own stated root-cause before writing any fix. (Chosen on the websec-validator field report, 2026-06-02.)

## Key Learnings

- **2026-08-28 — "Tests pass" on a major dependency bump means nothing if you only tested one Node version.** Merged dependabot's @babel/parser 7→8 bump after 1036/1036 passed locally — but locally means Node 24 only, and `@babel/parser@8.0.4` declares `engines: {node: '^22.18.0 || >=24.11.0'}`, silently dropping Node 18 (which this project's `engines` field and CI matrix both explicitly support). The mismatch doesn't fail `npm install` — it just produces wrong AST-parse results on the unsupported version, caught only when the release pipeline ran the real Node 18 test job and failed 20 tests. The pipeline's job-dependency gating worked exactly as designed (create-release/publish-npm/publish-pypi all correctly skipped), so nothing bad shipped — but the gap was avoidable. Fixed properly this time: `fnm install 18 20` locally, then verify a major bump across the FULL matrix a project's `engines` field + CI declare, not just the default local version, before pushing. Same rule applies to any future dependency bump on a project with a multi-version support matrix.
- **2026-08-28 — When reviewing an externally/bot-authored PR before merging, MERGE it onto current main in an isolated worktree and TIME the test run — don't trust the PR's own "tests pass" status.** PR #328's own new test passed, but merging it onto main and timing the full suite showed a 12x slowdown (118ms → 190s) on the exact file it modified. Root cause: the test called `scanSchemasDeep('/', {}, docTools, {})` with a malformed fixture (`docTools.openapi` missing `found: true`) — since the intended code path's early-return requires `openapi.found` truthy, the fixture fell through into a REAL recursive scan of `/`. A test that passes but never exercises the code it claims to is worse than an honest failure. Recipe: `git fetch origin pull/<n>/head:branch && git worktree add /tmp/x branch && cd /tmp/x && git merge origin/main --no-edit && ln -s <main>/node_modules . && npm test` (time it, don't just check exit code).
- **2026-08-28 — Jules (Google's coding agent) PRs "started by @raccioly" are the repo owner's OWN automated task runs, not third-party contributions.** Three of them (#324, #325, #329) were near-duplicate attempts at the same "add missing shared-ignore test coverage" task, submitted a week apart — evidence of a retry loop where the prior PR was never merged/closed. Diff their content directly before assuming independence: #325 was a strict subset of #324's coverage (same 3 functions, #324 additionally covered `walkFiles`), making #329/#325 fully redundant once #324 merges. Two more (#316, #327) were 0-file "checked, no update needed" PRs from a scheduled dependency-check task — safe to close outright once the real update (via dependabot) has merged.
- **2026-08-13 — When a fix changes what a bare filename gets replaced with (e.g. `f` → `doc.rel`), check whether that string feeds MESSAGE TEXT or a MATCHING KEY before applying it everywhere.** During the bug-253 second pass, blindly swapping bare basenames for full paths in `listCanonicalDocs`-based fixes broke two different things: (a) guard-validator finding `message`/`location` text is serialized verbatim into `guard --format json` — this repo's own self-scan is the regression oracle precisely because its `docs-canonical/` is flat, so ANY string-format change shows up as a diff even though nothing is "wrong"; the fix is `basename(doc.rel)` at each such site, matching the pre-existing flat-tree string exactly. (b) Some sites match by basename elsewhere in the SAME file (`impact.mjs`: `docsIndex.has(basename(f))`; `traceability.mjs`: `TRACE_MAP[docFile]`) — keying the Map/lookup by full path there silently breaks that unrelated lookup while ostensibly fixing visibility. Rule: the correct replacement value depends on what the ORIGINAL code produced for the FLAT case — reproduce that exact string (via `basename(d.rel)` if original was bare, or `d.rel` directly if original was already a `docs-canonical/${f}`-style template) — never assume "more specific path = strictly better."
- **2026-08-13 — `npm ci` does not care that `package-lock.json`'s top-level `"version"` field is stale relative to `package.json`.** Verified empirically (`npm ci` succeeds despite a 6-version gap: lock at 0.28.0, package.json bumped to 0.34.0) — it only validates the dependency tree, not root metadata. Not a release blocker; pre-existing drift, out of scope to fix opportunistically mid-release.
- **2026-08-13 — A machine's global npm config can break EVERY `npm install`, and it looks like it's the package's fault.** `min-release-age=<seconds>` in npm 11's global npmrc (this machine: `~/.local/share/fnm/node-versions/v24.18.0/installation/etc/npmrc`) triggered `ENOVERSIONS` on install of ANY package (verified with an unrelated package, `is-odd`) — not just docguard-cli. A prior agent session diagnosed this as "docguard-cli's own registry corruption" and was wrong; independent verification (`npm view <pkg> time --json` on the actual registry, `--min-release-age=0` override test) showed the registry data was clean and the failure was 100% local. This repo's own `.npmrc` already carries a comment about removing an identical `min-release-age=7d` project-level setting for the same class of npm-11 hard-fail — the fix (delete the line) is well-precedented. Lesson: don't trust another agent's root-cause attribution for an install failure without an independent repro — "it's the package" and "it's your npm config" produce identical symptoms.
- **2026-08-12 — Canonical-doc enumeration was hand-rolled and flat in 15 files.** `docs-canonical/` is repeatedly discovered with `resolve(projectDir, 'docs-canonical')` + a non-recursive `readdirSync(...).filter(f => f.endsWith('.md'))`. Nested canonical docs (`docs-canonical/01-architecture/MODULE-MAP.md`) are therefore invisible, and the failures are SILENT and INVERTED — docs-sync reports "not referenced in any canonical doc" for services documented in the very tree it failed to read, so the user cannot fix it by writing docs. Fixed in 5 sites via the new `listCanonicalDocs()` in `shared-ignore.mjs` (built on `walkFiles`, takes an `isIgnored` predicate as a param so `shared-ignore` stays a leaf module — `loadIgnorePatterns` lives in `shared.mjs`, and importing it would create the first shared↔shared-ignore edge). **14 files still carry the defect**: impact, init, llms, memory, trace, hooks, api-doc-smells, cross-reference, diff-suspicion, doc-quality, docs-coverage, generated-staleness, reference-existence, traceability. Grep recipe to find them: files matching `resolve(projectDir, 'docs-canonical')` that also call `readdirSync`.
- **2026-08-12 — DocGuard's own repo is a byte-diff regression oracle.** Its `docs-canonical/` is FLAT (8 docs, no subdirs), so any change to canonical-doc discovery must leave `guard --format json` and `score --tax --format json` byte-identical apart from `durationMs` and `timestamp`. Recipe: capture both to files before the change, `diff <(grep -v '"durationMs"\|"timestamp"' base) <(... after)`. This caught nothing this time, which is exactly the point — it converts "I think it's backward compatible" into evidence.
- **2026-08-12 — Verify a regression test by running it against pristine HEAD.** `git archive HEAD cli | tar -x -C <scratch>` gives an unmodified copy without touching the worktree (no stash, no worktree add). Copy the new test into `<scratch>/tests/` — its `../cli/...` imports resolve to the old code. Needs `git show HEAD:package.json > <scratch>/package.json`, because `upgrade.mjs` reads package.json at import time and the whole suite fails to load without it. Five of the twelve new tests failed there; the one that passed on both sides is the over-correction guard.

- **2026-07-15 (market research — AI-age repo documentation, pre-v0.33 release)** — (1) ETH Zurich study (138 repos/5,694 agent PRs, HN 47034087/47295454): the POPULAR AGENTS.md style HURTS agent performance; best case ≈ +4%. Practitioner consensus (HN 47481682, 47938417): "explicit instructions are routinely ignored — programmatic enforcement (AST checks, pre-commit hooks) is the only reliable path." This is DocGuard's thesis verbatim → now cited in README Why-DocGuard + PHILOSOPHY Academic Foundations. (2) llms.txt reality (ppc.land, aeoengine 2026): web AI crawlers essentially never fetch it (97% of files get zero requests; Google explicitly won't support it) — but IDE agents (Cursor/Claude Code/Copilot) and MCP DO use it. Don't oversell llms.txt for SEO/citations; sell it for in-repo agent context. (3) Docs-tooling market direction (Mintlify/GitBook/Swimm 2026 roundups): "weight the auto-sync story more heavily than generation quality" — buyers care about staying-true, not first-draft. DocGuard's sync+guard is the right bet. (4) Stack Overflow blog 2026: AI coding → 4x code cloning, less refactoring — supports architecture/traceability governance pitch. Dogfooding gaps FIXED this session: ci.yml now smokes report/ci/trend/junit/baseline; FAQ gained the brownfield-adoption Q; configuration.md gained the baseline key; README What's-New was rotting at "v0.16→v0.19" (!) — refreshed to v0.29→v0.33.
- **Ignore wiring map (as of fcc7264):** all `shared-ignore` consumers — incl. `routes.mjs:90` and `todo-tracking.mjs:307,383` — call `shouldIgnore`. `project-type.mjs` did NOT until the B1b fix (2026-06-02): its `walk()` in `findManifests` now honors config.ignore (prunes dirs + skips manifest files via shouldIgnore). It detects frameworks from **dependency manifests** (`has(deps,'express'/'flask')`), so before the fix it ingested fixture `package.json`/`requirements.txt`. Two different mechanisms: glob filtering on results vs. `DEFAULT_IGNORE_DIRS` (bare dir-name Set) during walks.
- **`globToRegex` (shared-ignore.mjs:115) silently mishandles trailing-slash dir patterns.** Verified by execution: `buildIgnoreFilter(['tests/'])('tests/fixtures/x.js')` → `false`; `['tests']` and `['tests/**']` → `true`. Gitignore-style `dir/` (the natural form) matches nothing. This makes ignore look wired-but-broken across every consumer. (Fixed 2026-06-02 by stripping the trailing slash.)
- **The dispatcher (docguard.mjs) installs DocGuard's own agent tooling as a side effect.** `ensureSkills()` runs on every command except `setup`/`init` and except when `headless` (= jsonMode||write||checkOnly||changedOnly||quiet||plan after the B4 fix). It writes `.agent/skills` + `.agent/commands` and can spawn the `specify` CLI (→ `.specify/` + pip/npm). So read-shaped commands must be added to the `headless` set to stay side-effect-free. `headless` is consumed in exactly two places: the banner and this ensureSkills gate.
- **Repo/package name mismatch trips Claude's auto-mode typosquat classifier.** Repo is `raccioly/docguard`; published package is `docguard-cli` (npm + PyPI); bin/command is `docguard`. When an agent autonomously runs `npm i -g docguard-cli` after the user only named `raccioly/docguard`, the `-cli` suffix reads as a typosquat and the install is denied. This is NOT a bug — provenance is fully wired (`repository`/`homepage`/`bugs` → the repo) and the package runs **no install scripts** (only `prepublishOnly`). Resolutions, cheapest first: install from local checkout (`npm i -g .`), add `--ignore-scripts`, a `Bash(npm i -g docguard-cli*)` permission rule, or just approve. Do NOT rename the repo/package to "fix" this — blast radius hits the spec-kit catalog, badges, and external links. Mitigation shipped 2026-06-10: a Quick Start note in README.md stating the repo↔package↔command relationship explicitly.
- **Detection-ignore vs structural-ignore are SEPARATE concerns.** `shared-ignore.mjs` now has `DEFAULT_DETECTION_IGNORE_DIRS` + `isNonProductDir`/`isNonProductPath` (fixtures/tests/examples/testdata/samples) applied ONLY to surface-detection scanners (`project-type.findManifests`, `routes.mjs` result filter, `shared-source.grepEnvUsage`) — NEVER to guard's structural validators (a user's real `examples/` still counts for coverage). Default-on, overridable via `config.detection.includeNonProduct`. This is the first-run fix for fixture contamination; honoring `config.ignore` alone (v0.25) wasn't enough because a first run has no `.docguardignore`.
- **`ensureSkills` (scaffolding) must only run for setup/init/generate + `init --with`.** Read-only commands are listed in `READ_ONLY_COMMANDS` in `docguard.mjs` and are gated out (plus the headless flags). A validate/report command writing `.agent/.specify` is a P0 trust break. When adding a new read/report command, add it to that set.
- **Metrics-Consistency is subject-bound + fail-closed.** It validates a "N checks/validators" number ONLY when the containing line references "docguard" (`isDocguardBound`), stamps `actualSource` provenance on every fix, and `applyReplaceCount` REFUSES to write without `actualSource` and only rewrites DocGuard-bound lines. Never loosen this — an unbound auto-overwrite corrupts correct numbers (proof-harness "10 checks" → DocGuard's "86").
- **`flags.format` defaults to `'text'` globally (docguard.mjs:~302).** So plan/agent commands can't distinguish "unset" from "explicit text". Convention: text summary is the default, `--format json` is the machine artifact (matches every other command). `generate --plan` and the new `agent` command are headless (own header, no global banner, no ensureSkills).
- **Pre-filled code-truth + `docguard agent`.** `cli/scanners/inventory.mjs` (`scanComponents` = top-level source modules with single-package-wrapper descent; `scanTestInventory` = per-file test-case counts, multi-language) feed pre-filled ARCHITECTURE Component Map + a TEST-SPEC inventory section (generate previously never emitted TEST-SPEC). `cli/commands/agent.mjs` (`buildAgentTaskGraph`, pure) emits an ordered task graph (config→canonical-docs→verify) with kind code-truth|human-judgment, prefilled content, grounding, per-task `acceptance.verify`, and confidence. `--profile` overrides config to preview without `init`.

- **Findings architecture (v0.27, `cli/findings.mjs`).** A validator can emit structured `Finding[]` — `{code, validator, severity, confidence, message, location, suggestion, reportable, redactedContext}` — and return `resultFromFindings(findings, {passed,total})`, which DERIVES the legacy `{errors,warnings,passed,total}` from the same array AND attaches `findings`. Backward-compatible: guard renders `findings` richly (inline `→ suggestion`, `[CODE]` tag, low-confidence marker) when present, else falls back to the error/warning strings (`renderableItems()` in guard.mjs). Counts/exit-codes still come from errors/warnings — never duplicated. Only `security` is fully migrated so far; the rest inherit the fallback. Stable codes live in the `CODES` registry (append-only — a published code is public surface). `explain <CODE>` and inline `// docguard:ignore <CODE>` both key off it.
- **Inline suppression** (`suppressesCode`/`lineSuppresses` in findings.mjs): `// docguard:ignore SEC001`, `docguard:ignore SEC*`, `docguard:ignore-secret` (any SEC*), bare `docguard:ignore` (any), comma lists — checked on the flagged line AND the line above. The security validator calls `lineSuppresses(code, matchLine, prevLine)` before emitting.
- **guard's stable contract (v0.27).** `runGuardInternal` now additionally returns `findings` (flattened), `reportable` (low-confidence subset), and `nextStep`. These are additive — existing JSON consumers unaffected. The "always suggest next" footer in `runGuard` derives from these: diagnose pointer on non-PASS, workflow `score` pointer on PASS, `docguard feedback` nudge when reportable findings exist, and a read-only `docguard init` skills nudge (never writes).
- **`docguard feedback` (v0.27).** Local-first: writes `.docguard/feedback/<code>-<id>.json` (deterministic id via djb2 hash — no Date/random) + prints a prefilled GitHub issue URL CAPPED at 1800 chars and REDACTED (basename:line only, no literal values — security builds `redactedContext` that omits the matched value). Reuses the commit-3b600fd lesson (oversized URL overflows GitHub's ~8 KB limit). It's in `READ_ONLY_COMMANDS` (so it never scaffolds skills) even though it writes its own feedback dir.
- **`score.mjs` can't be unit-tested with `{}`** — `runScoreInternal({})` throws (`calcStructureScore` reads `config.requiredFiles.canonical`). Extract testable seams instead: the test-runner detection is now `export function detectTestRunner(dir, config)` (recognizes vitest-in-vite.config via `vitest/config` import or a `test:` block, `scripts.test` runner names, and workspace-subdir configs). Pattern for future score tests: pull the unit out, don't feed the whole pipeline a half-config.

- **`verify --semantic` design (v0.28, `cli/scanners/semantic-claims.mjs` + `cli/commands/verify.mjs`).** DocGuard is zero-dependency and NEVER calls an LLM itself. So the semantic-drift check (field report #5 — documented numbers/enums vs code) is an EXTRACTOR, not an LLM call: `extractSemanticClaims` finds documented numbers-with-units (days/ms/req-s/GSIs/roles via `NUMBER_PATTERNS`) + enum lists (2+ UPPER_SNAKE in a status/state context) + the nearest cited code path (±1 line window), and `buildSemanticVerifyTasks` emits an agent task list. The agent running docguard does the comparison. Same division of labour as `docguard agent`. Precision-first: code-fenced numbers, version strings, and dates are excluded by design.
- **`sync --tests` safety scope (v0.28, `cli/commands/sync-tests.mjs`).** The TEST-SPEC Source-to-Test Map is HAND-maintained, so the reconciler does only two non-destructive ops: remove rows whose SOURCE file is gone, append co-located (`same dir` or sibling `__tests__/`) source↔test pairs absent from the table. Ghost TEST refs (source exists, test gone) are REPORTED, never auto-edited — blanking a curated status/notes cell is too destructive (the Test-Spec validator already warns). Pairing is co-located-only to avoid cross-directory basename collisions polluting the table.
- **Dynamic-import cycle handling (v0.28).** `architecture.mjs extractImports` now returns `{spec, dynamic}`. `graph.fileMap` (cycle DFS adjacency) gets STATIC edges only; `graph.edges` (layer checks) keeps all. A runtime `await import()` is the canonical cycle BREAK, not a load-time edge.
- **API-Surface spec-vs-route (v0.28).** When an OpenAPI spec exists it's the authoritative surface, so the API-REFERENCE doc reconciles against the SPEC — a phantom spec endpoint sails through. `computeSpecVsRouteDrift` diffs spec endpoints against `scanRoutesDeep` results (reusing `compareEndpoints` for method/path normalization), but ONLY when routes are scannable (`routes.length > 0`) — else it can't tell "no route" from "scanner blind" and would false-positive every endpoint.
- **ALCOA "Accurate" honesty + `config.collections` (Unreleased, field report #6).** The `Accurate` ALCOA+ pillar was structural-only (`scores.drift >= 80 && scores.docQuality >= 50`, score.mjs) — it read ✅100% while a watched doc stated a wrong count. Now 3-state: met / `unverified` (cyan 🔍 = structure OK but documented facts unchecked vs code, derived from `extractSemanticClaims().length`) / unmet. `unverified` counts as not-met for the ALCOA % (stops overclaiming) but is **display-only** — the gating CDD grade (`totalScore` via `calcScore`, read by badge/ci) is computed BEFORE the ALCOA block and never fed by it, so it's untouched. NEW deterministic detection: `config.collections` (noun→glob, metrics-consistency.mjs) flags "16 extractors" vs the real file count, in `guard`, no LLM — a declared noun IS the opt-in binding so it skips the `docguard`-subject-bind the built-in checks/validators counts need (`requireBind:false`); reserved nouns checks/validators/tests keep built-in meaning; a 0-match glob is skipped (never asserts "0"); minimal inline `globToRegExp`/`countGlob` (zero-dep). guard also surfaces the existing semantic-claim extractor count + a doc-coverage map (canonical/tracked/ignored/outside-any-tier) as NON-gating notices (calm: count always, file list only `--verbose`). The literal field-report bug was just a missing noun ("extractors") in the `NUMBER_PATTERNS` count vocab. Complements `surfaceSync` (WHICH members drift) with a count (HOW MANY).
- **Findings migration 24/24 COMPLETE + generate split (2026-07-03, "nothing left behind" batch):** CODES registry = 91 codes / 21 prefixes (STR,CHG,MET,FRS,ENV,TSP,DRF,DSY,DDF,DCV,MDS,TRC,TDO,SCH,ARC,CSY,SPK,XRF,GST,SSY,API,DQ + SEC). Executed via 4 parallel subagents on DISJOINT validator files with findings.mjs (the shared CODES file) reserved to the orchestrator — zero edit collisions; each agent returned code definitions as JSON for central append. Gotchas encoded: (1) several tests strict-deepEqual whole result objects on N/A/early-return paths — those paths must keep the LITERAL legacy shape (no findings/fixes keys); real validation paths return via resultFromFindings. (2) spec-kit's validator file is a re-export SHIM — the implementation (and migration) lives in cli/scanners/speckit.mjs. (3) `generateFromSpecKit` in speckit.mjs has a `results.errors.push` that is NOT a validator contract (generator result {generated,skipped,errors}) — do not "migrate" it. (4) api-surface/doc-quality were fully legacy despite the "partial" label in the review. (5) freshness keeps its array-of-{status,code,doc,message} contract; guard's adapter builds the findings (FRS001–005). generate.mjs split: 1530→559 + writers/generate-io.mjs (142) + writers/doc-generators.mjs (853), pure code motion, generateERDiagram import moved with generateDataModel. Watch-test flake root cause was a fixed 2s poll cap vs CLI startup — fixed with a 15s waitFor deadline, NOT retries. Full suite 835/835 after everything.
- **v0.29 consolidation executed (2026-07-03):** the review's P0/P1 items are DONE, not pending. `shared-ignore.mjs` now owns the ONE recursive walker (`walkFiles(dir, cb, {ignoreDirs, keepDot, skipDotEntries, onError})` — returns a completeness boolean) and the ONE anchored glob compiler (`compileGlob`: `**\/`, `**`, `*`, `?`, `{a,b}`; `globMatch` and `countGlobFiles` build on it). 16 call sites migrated as thin wrappers preserving exact semantics (13 validators + guard coverage + diff + generate); ignore-side `globToRegex` deliberately left separate (unanchored substring contract, own bug history). `countGlobFiles` returns -1 on incomplete walks (fail-safe; bug-147). Findings tranche 2: structure/doc-sections (STR001–003), changelog (CHG001–003), metrics-consistency (MET001–002) → 7 of 24 validators on findings; messages kept byte-identical so tests pass unchanged (one strict deepEqual updated to expect the additive `findings: []`). CONTRIBUTING now carries the surface rule + findings rule. Full suite 835/835 including the 20 AST tests.
- **Project-review findings (2026-07-02 sweep — durable facts for future work):** (1) DUPLICATION MAP: three glob→regex implementations exist (`shared-ignore.mjs:155` globToRegex, `shared-ignore.mjs:224` globToMatchRegex, `metrics-consistency.mjs` globToRegExp/countGlob — the third added 2026-06-23 for collections; consolidation candidate) and ~13 validators each carry their own recursive dir-walker + local IGNORE_DIRS copy — a shared `walkFiles` in shared-ignore.mjs is the fix. (2) FINDINGS MIGRATION: only security.mjs fully emits `Finding[]`; api-surface + doc-quality partial; ~20 validators still legacy strings — guard's rich rendering (suggestions/codes/confidence) only benefits 3 validators. (3) `docs-canonical/SURFACE-AUDIT.md` is a HISTORICAL point-in-time audit (2026-05-26, v0.18.1); its recommendations SHIPPED (canonical-sync in v0.19, consolidation in v0.20) — do NOT treat its counts as current truth; README counts are machine-governed by Canonical-Sync since v0.19. It needs a "historical — resolved" banner. (4) STANDARD.md §8 lists 8 validators vs 24 actually run — stale spec section. (5) Size hotspots: generate.mjs 1551 lines, score.mjs 1063, fix.mjs 835, guard.mjs 829 (guard's separation judged reasonable; generate is the split candidate). (6) Packaging/CI is clean: 1 pinned dep (@babel/parser, graceful regex fallback via astTierAvailable), clean tarball, node 18/20/22 matrix, versions auto-synced npm→PyPI; only blemishes are pyproject.toml:8 "Zero dependencies" wording and tracked scratch files test-draft.js/test-metrics.js at root.
- **Google Jules sandbox VM specifics (2026-07-06, bug-200).** (1) The Jules VM sets `git config core.hooksPath /dev/null` to disable hooks. Any code that resolves the git hooks dir via `git rev-parse --git-path hooks` gets back the literal `/dev/null`; `resolve(dir,'/dev/null')` + `<hooksDir>/pre-commit` = `ENOTDIR`. `getHooksDir` (shared-git.mjs) now guards `out !== '/dev/null'` and falls through to the `.git/hooks` check. Regression test lives in tests/hooks.test.mjs (real repo + `git config core.hooksPath /dev/null`). (2) `.jules-setup.sh` (pasted into Jules dashboard → Repo → Configuration → Initial Setup) MUST leave the working tree clean or Jules aborts "Working tree is dirty" AFTER setup. Two traps: `npm install` re-resolves and rewrites package-lock.json → use `npm ci`; and `docguard --version` runs ensureSkills which regenerates the **tracked** `.agent/skills/*.md` (version-frontmatter churn), so `git clean -fd` (untracked-only) is INSUFFICIENT — end the script with `git reset --hard HEAD && git clean -fd` (`-fd` respects .gitignore so node_modules survives). (3) `.agent/skills/*.md` being tracked-yet-regenerated is the root cause of BOTH the dirty-tree failures and the recurring Jules-PR `.agent` noise — a future option is to gitignore + `git rm --cached` them, but the setup-script reset handles the symptom robustly.
- **Doc-home resolution is now ONE source of truth: `resolveDocDirs(projectDir, config)` in shared.mjs (Unreleased).** Both the claim scanner (`metrics-consistency.findMarkdownFiles`) and guard's coverage map call it, so "tracked" provably means "actually scanned" — previously the two used different hardcoded dir lists and could disagree (a `docs-implementation/` file showed "tracked" but wasn't scanned). It auto-detects conventional doc-home NAMES that exist (`DEFAULT_DOC_DIRS`: docs/doc/documentation/guides/guide/handbook/manual/wiki/docs-canonical/docs-implementation/extensions + Docusaurus `website/docs`); `config.docs.dirs` EXTENDS (never replaces) that set; `.docguardignore` is how you EXCLUDE a conventional dir. KEY DISTINCTION (reconciles with the wu-whatsappinbox scoping fix): auto-tracking a folder literally named `documentation/` is NOT the same as the arbitrary-subdir walk that was removed — `security/wolf-archive/**/memory.md` is still never scanned. The existing test only asserted arbitrary-subdir exclusion + CONFIGURED-doc inclusion; nothing asserted an unconfigured `documentation/` must be ignored, so auto-detection is consistent with intent, not a reversal.

## Do-Not-Repeat

- **2026-07-15** — `docguard ci` was silently routed through `runInit --with ci` since v0.20: the pipeline gate scaffolded missing docs into the workspace and corrupted `--format json` stdout with init chrome (bug-228). Un-deprecated in v0.33 (direct runCI dispatch). Lesson: when a command is deprecated-with-routing, verify the ROUTE preserves the command's contract (read-only? machine stdout?) — a passing test suite didn't catch it because the consolidation test only asserted the warning text, not the side effects.
- **2026-07-03** — Adding a machine output format to a command is TWO changes, not one: the format branch in the command AND the dispatcher's `jsonMode` gate (docguard.mjs ~line 515). `guard --format sarif` initially emitted the banner + ensureSkills chrome INTO the SARIF stream because `jsonMode` only recognized `'json'` — caught by the spawn test's no-ANSI assertion before ship. Any future format (`junit`, `csv`, …) must join that gate or stdout is corrupted for parsers. Same class as the READ_ONLY_COMMANDS gotcha (learning #17).
- **2026-07-03** — `agents --sync` marker contract (v0.29): generated agent-family files carry `docguard:agents-sync source=AGENTS.md hash=<sha256:16>` (JSON targets: `_docguardSync` field instead — comments break JSON; .mdc: marker goes AFTER frontmatter, Cursor requires frontmatter first-bytes). Semantics: marked = ours to regenerate; unmarked-existing = user content, NEVER overwrite without --force; --check gates ONLY marked files (exit 2 stale, unmarked/absent = "unmanaged", not failure). If a future validator wants to enforce family freshness in guard, reuse extractMarkerHash/sourceHash from agents.mjs — don't invent a second marker.

- **2026-07-03** — NEVER write a literal `**/` inside a JSDoc/block comment: the `*/` terminates the comment and the rest becomes bare source → module-level SyntaxError that fails EVERY test file at load ("not ok" on ~60 files is the tell — shared-import parse error, not test logic). Escape as `**\/` (the file's existing convention) and diagnose with `node --check <file>`.
- **2026-07-03** — The per-validator IGNORE_DIRS sets are NOT interchangeable copies: they carry intentional variance (drift excludes `cli/` — DocGuard's own source has `DRIFT:` in regexes; docs-sync excludes `__tests__`; freshness excluded templates/configs/Research) and two walkers have load-bearing dot-entry EXCEPTIONS (security must scan `.env`; traceability keeps `.env*`, `.gitignore`, `.github/`). When touching traversal, preserve each validator's set via `opts.ignoreDirs` + `keepDot` on the shared walker — do NOT unify onto DEFAULT_IGNORE_DIRS.
- **2026-07-03** — This machine's main tree has NO node_modules; a worktree symlink to it is DANGLING. Run `npm ci` in the worktree (4 packages, <1s) before trusting the AST-tier tests — 20 tests silently depended on @babel/parser being installed.

- **2026-06-22** — A CLI command that emits machine output and exits must NOT `console.log(payload); process.exit(code)`. To a PIPE, a payload over ~8 KB flushes asynchronously and process.exit() truncates it mid-write → a consumer gets "Unterminated string in JSON". Use `process.exitCode = code` + `process.stdout.write(payload)` then `return` (Node drains stdout, exits naturally). Writing to a FILE is synchronous so it NEVER reproduces — you must test through a real pipe (spawnSync captures stdout via a pipe; that's why the test caught it and the manual file-redirect repro didn't). Fixed in guard.mjs; audit other `--format json` exits if touched.
- **2026-06-22** — When adding a new internal module under `cli/commands/` that is NOT a registered command (e.g. `sync-tests.mjs` backing `sync --tests`), add it to `.docguard.json` surfaceSync `commands.ignore` — else Surface-Sync flags it as "in code but missing from README" and Canonical-Sync miscounts "ships N commands". Real commands go in the README table + bump the count + the mermaid `Commands (N)`.


- **2026-06-19** — A "prose vs secret" heuristic must NOT treat terminal punctuation alone as a prose signal. First cut of `looksLikeProse` flagged any value ending in `.!?` as natural language; strong passwords routinely end in `!` (`"SuperSecretPassword!"`), so a real one-token secret got downgraded to a low-confidence warning — the existing `security.test.mjs` caught it (2 errors → 1). Rule that works: prose = ≥3 words, OR a 2-word fragment ending in `.!?`. When adding a downgrade heuristic to a SECURITY check, bias toward keeping the error (a false-negative hides a secret) and write the test against a single-token-secret control.
- **2026-06-19** — When adding a new `docguard` command, the repo guards ITSELF: Surface-Sync flags it missing from the README commands table and Canonical-Sync flags the "ships N commands" count + the architecture mermaid `Commands (N)`. Update all three (README prose count, README table, mermaid) in the same change or DocGuard's own guard fails. `feedback` made it 14 → 16 (there was a pre-existing 1-off; real count was already 15).

- **2026-06-09** — spec-kit extension manifest: an `alias` MUST differ from its command `name`. spec-kit registers name + every alias in one namespace, so `aliases: ["speckit.docguard.guard"]` on a command named `speckit.docguard.guard` is a self-collision → "Duplicate command or alias" → install/update aborts + rolls back. (Issue #229; broke v0.23.0–v0.25.0.) If an alias adds nothing, omit it.
- **2026-06-09** — Do NOT publish to the spec-kit community catalog via a direct PR against `github/spec-kit` (editing `catalog.community.json`). Maintainers reject that path (github/spec-kit#2707; the publishing guide is explicit). Catalog add/update MUST go through the **Extension Submission issue template** (`.github/ISSUE_TEMPLATE/extension_submission.yml`), and it must be submitted **through the form UI** — the template's `labels:`/auto-assign only fire on a real form submission, NOT on an API `gh issue create` (which also can't apply labels without triage rights we lack on github/spec-kit). So our triggers do NOT call github/spec-kit at all: `.github/scripts/speckit-submission.py` emits a **prefilled `issues/new?template=…` URL** (`--url`) + a paste-fallback body (`--body`), and the workflows open a one-click reminder issue **in our own repo** (release.yml `sync-catalog` job + manual sync-speckit-catalog.yml). Ricardo clicks → submits the form. When the upstream form fields change, update `speckit-submission.py` to match. `SPECKIT_PR_TOKEN` is no longer needed by these workflows.


- **2026-06-02** — Don't trust a bug report's stated root cause. The websec B1 report blamed "scanners/validators skip the ignore file"; the real defect was the trailing-slash glob (above). Fixing per the report ("centralize ignore handling") would have changed nothing AND left a false-green — the exact failure mode this tool exists to prevent.
- **2026-06-10** — A "fix" that patches the reported REPRO but not the general CLASS is a false-green: the v0.25.0 work claimed all of field-report-1 B1–B7 "fixed + tested", yet field-report-2 hit the same issues on v0.25.1 because (a) fixture-ignore only worked WITH a `.docguardignore` (first run has none), (b) the side-effect fix only covered `generate --plan`, not `guard`, (c) B7 silenced guard by widening requiredFiles instead of making generate respect the profile. Worse, **two tests CODIFIED the bugs** (`project-type.test.mjs` asserted fixtures SHOULD leak with no ignore; `metrics-consistency.test.mjs` asserted subject-blind matching). Lesson: when fixing, fix the class, write the regression test against the AGENT'S ACTUAL SCENARIO (first run, no config), and audit whether an existing green test is actually encoding the bug.
- **2026-06-02** — Don't assume a documented CLI flag is wired. `--fix` is documented (`docguard.mjs:111` "Auto-create missing files from templates") and set (`:186`) but read **nowhere** — `init.mjs` never consumes `flags.fix`. `grep` the flag's *consumption*, not just its declaration.
- **2026-07-03** — Release-flow gotchas (v0.29.0): (1) **main has `required_linear_history: true`** — the "Create a merge commit" button is DISABLED by branch protection; releases must land via **rebase merge** (`gh pr merge --rebase`, keeps the commit series) or squash. Advising "merge commit" confuses the user with a greyed-out button. (2) The auto-mode classifier blocks BOTH direct push to main AND self-merging an agent-created PR under a general "release" instruction — it requires the user to either click merge or EXPLICITLY say "merge it"; once the user says so, `gh pr merge` goes through. (3) PR check summaries can read "5/7" with 0 failures — conditional jobs (`publish-check`, `osv-scan-scheduled`) report as *skipped*, which the summary counts against the total; not a blocker. (4) Full pipeline order confirmed working: rebase-merge → release.yml on package.json path → tag+GitHub Release (extension ZIP asset) → npm → PyPI (sed-synced) → sync-catalog reminder issue.
- **2026-06-05** — Release CI gotchas (cost a failed v0.25.0 publish, then fixed): (1) `.github/workflows/release.yml`'s `test` job was MISSING `npm ci`, so `@babel/parser` (a regular dep) was absent and all 20 AST tests failed. It stayed latent because `detect-version` skips the job unless a release is due — so the job had literally never run. (2) `release.yml` is `paths`-filtered to `package.json`, so a workflow-only fix commit does NOT re-trigger it — use `gh workflow run release.yml --ref main` (the `workflow_dispatch` recovery trigger). (3) The `docguard watch` and `auto-fix prompts` CLI-spawn tests are CI-FLAKY (failed once on a2684e4, passed on identical-code re-run + locally) — a re-run, not a real failure.

## Decision Log

- **2026-07-15 (pre-release adversarial review — 12 findings, all fixed + regression-tested)** — Independent agent review of the v0.33 batch caught 3 HIGH: (H1) restoring `ci` as first-class REQUIRES adding it to READ_ONLY_COMMANDS too — dispatch and the ensureSkills gate are separate lists (bug-247); (H2) a count-less baseline Set suppresses UNLIMITED future same-fingerprint findings — same code+file+message-shape collides (two secrets in one file) → format v2 fingerprint→count with per-run budget (bug-248); (H3) surfaces that CONSUME runGuardInternal (report/ci) must explicitly propagate baselineSuppressed — the data flows through but disclosure doesn't (bug-249). MEDIUMs (bug-250): junit failure detection must key off v.status too (crash path has string errors, no findings); ci must gate on effective* like guard; ci status must fold in threshold/fail-on-warning (was logging PASS for failed gates); integrity hash must exclude anything mtime/wall-clock-derived (ALCOA Contemporaneous) or 'same commit ⇒ same hash' is false on fresh clones. META-LESSON: every finding was in code all 1012 tests passed on — the tests asserted the happy paths I designed; the reviewer attacked the paths I didn't. Adversarial review before release is not optional for gate/evidence features.
- **2026-07-15 (iteration 4 — adoption baseline)** — `.docguard.baseline.json` at REPO ROOT (committed; .docguard/ is gitignored so a baseline there would be per-checkout and useless). Fingerprint = sha256(code|location-path-sans-line|message-with-digits→#) first 16 hex — survives line churn + volatile counts. Applied INSIDE runGuardInternal before all tallies (guard/ci/report/sarif/junit/mcp uniform); only findings-backed results (errors+warnings === findings.length guard) — string-only legacy validators can't be fingerprinted. Malformed baseline = absent (never un-gates CI). GOTCHA FOUND: writing the baseline immediately tripped DCV001 (undocumented config file) about the baseline itself — DocGuard's own files (.docguard.json/.docguardignore/.docguard.baseline.json) now exempt in docs-coverage COMMON_DOTFILES. TEST GOTCHA: a bare fixture's security validator flags NOTHING for a hardcoded secret in src/ (no scan without more setup) — use delete-a-doc (new STR001 path fingerprint) as the deterministic new-finding vector. Config: "baseline": false + --no-baseline; schema prop added WITHOUT bumping CURRENT_SCHEMA_VERSION (additive optional, historical precedent). Empirical: taxes_helper 1→0 exit, 49 fingerprints/64 findings suppressed, --no-baseline restores.
- **2026-07-15 (iteration 3 — integration formats)** — `guard --format junit` (writers/junit.mjs: testcase-per-validator, error→<failure>, warn→<system-out>, na→<skipped/>; exit codes = json/sarif branches; joined the docguard.mjs jsonMode gate per the Do-Not-Repeat rule — that gate now reads json|sarif|junit) + CI-RECIPES Recipe 1b (GitLab artifacts:reports:junit, Jenkinsfile). `docguard_report` MCP tool (buildReport reused; tool count 5→6 — BOTH mcp.test.mjs and mcp-http.test.mjs assert the tool list, update both). Empirical: valid XML on globalshares (27 tests/0 fail/10 skipped). Tests 1001→1005. NEXT-ITERATION candidate chosen: `guard --baseline` (ESLint/semgrep-style adoption baseline — legacy repos adopt without day-one failures, only NEW findings gate); REJECTED: report --html (marginal vs markdown), ci --webhook (breaks the nothing-leaves-your-infra claim).
- **2026-07-15 (v0.33 enterprise-evidence batch — /loop session)** — GitBook gap analysis → enterprise features, all into [Unreleased]: (1) **`docguard report`** — compliance-evidence bundle (guard+score+ALCOA+findings-by-code+fix-history, git-stamped, sha256 integrity hash excluding generatedAt so same tree = same hash; ALWAYS exit 0 — evidence never self-censors; headless in both formats). computeAlcoaCompliance exported from score.mjs; getHeadInfo added to shared-git. (2) **Score history** — writers/history.mjs (JSONL, append O(1), silent-fail, 256KB trim), `ci` appends per run (`--no-history` opt-out), `score --trend` sparkline+delta+last-10; CI-RECIPES Recipe 4b (actions/cache persistence). (3) **`ci` un-deprecated + pipe-truncation fix** (bug-228). Command count 18→20 (report, ci) — README prose+table+mermaid all updated (the three self-guard flags SSY002/CSY002/CSY004 fired exactly as cerebrum predicted, then cleared). (4) Phantom-completion was NOT built — already covered by `trace --features` task-evidence (checked tasks naming nonexistent files); verified before building, avoided a redundant validator + self-count ripple. (5) COMPARISONS.md gained "CDD vs Docs Platforms" (positioning: platforms publish, DocGuard proves; audit-trail differentiator — GitBook/ReadMe ship no audit logs at any tier). REJECTED for scope: hosted dashboard (Phase 5), multi-repo org sweep (defer until demand), PDF export. Tests 984→1001 (7 report + 10 history; one v020-consolidation test rewritten to assert the new ci contract). EMPIRICAL PASS (iteration 2, read-only, per the mandated method): `report --format json` valid + sensible on hugocross_revamp (98 A+, 0E/4W), taxes_helper (35 D, 6E/58W — FAIL state still exits 0), stablecoin-strategy-lab (92 A), globalshares_revamp (98 A+, 167W); markdown clean; `score --trend` hint correct with no history; `ci --no-history` exits 2 on WARN and writes nothing. ORG SWEEP: CUT — a 3-line shell loop over `ci --format json` covers it (documented as CI-RECIPES Recipe 4c); first-class cross-repo stays Phase 5 dashboard.
- **2026-07-11 (v0.32.0 assembled — full "do it all" session)** — After the detection batch, the user approved the whole follow-on list; everything landed on PR #314 (branch claude/graphify-docguard-review-8a64ca, 10 commits): housekeeping (doc tiers 30→11 outside; TEST-SPEC 10s→30s claim), `hooks --claude` agent nudge (opt-in PostToolUse in project .claude/settings.json; runtime `nudge-hook` is headless + in READ_ONLY_COMMANDS; silent-on-error; 30-min/file throttle in .docguard/nudge-state.json), MCP Streamable HTTP (`--transport http`; non-loopback bind REFUSES without --api-key; shared dispatchMessage for both transports), `impact --prs` (pure computeDocConflicts + gh at the edge), VALIDATION.md (corpus repos described by stack, never client names — public repo). NEW BUG CLASS FOUND (bug-212 family): scanners that enumerate docs WITHOUT loadIgnorePatterns — fixed semantic-claims + agent-readability; grep for readdirSync('docs-canonical') when adding any doc scanner. GOTCHA: `hooks` is a DEPRECATED alias routed through init --with hooks; `hooks --claude` bypasses via a flags.claude branch + deprecation-warning exemption. GOTCHA: the auto-mode classifier BLOCKED `gh pr merge` (self-approval) exactly as predicted — release is parked on the user clicking merge on #314; after merge: watch release.yml, bump homebrew-tap sha256, ff-only pull the main tree, submit spec-kit catalog form, remind mcp-publisher (server.json already at 0.32.0). Addressability metric left at 43% DELIBERATELY — the remaining >120-tok sections are schema tables/mermaid; fragmenting them = metric-gaming (ALCOA honesty lesson).
- **2026-07-11 (graphify batch APPLIED — [Unreleased])** — Items from the graphify review shipped same-day: (1) REF002 ADR-citation check inside reference-existence (validator count stays 27 — deliberate, avoids the self-count split); comment-gate + non-product scoping + integer number compare; RFC out of scope. (2) Wikilinks in cross-reference (XRF001/002) + impact blast radius, gated on evidence (`.obsidian/` or ≥1 resolving target) because `[[name]]` is also a non-file convention (our own memory links!); plus query-string strip + angle-bracket targets. (3) `impact` indirect mode: reverse-import BFS (2 hops) over the EXPORTED `buildImportGraph` from architecture.mjs (one builder); hub suppression at out-degree >15 was REQUIRED — without it cli/docguard.mjs flagged 7 docs on a 3-commit range, with it 2 genuine chains; `--no-indirect` opt-out. (4) Traceability consumes committed `graphify-out/graph.json` as TRC002 evidence — EXTRACTED edges only, linked file must exist, evidence-only (never a finding). Item 5 (cache portability) audited = already clean (.docguard/ gitignored, mtime-keyed cache is inherently local). Item 6 (PreToolUse nudge hooks) DEFERRED — writes to user agent configs, trust-sensitive class, needs Ricardo's call. Empirical read-only validation on 5 real repos: 0 FPs everywhere; indirect impact gave 6 explainable chains on globalshares (74 changed files) and 1 on hugocross. Tests 937→959. GOTCHAS: (a) writing `ADR-<digits>` in DocGuard's own comments trips REF002 — use ADR-NNN placeholders (bug-211); (b) referencing a dotfile like `.obsidian` in validator code trips DCV004 until a user-facing doc (README table) mentions it — CHANGELOG doesn't count.
- **2026-07-11 (Graphify-Labs/graphify review — idea harvest, no code)** — Reviewed graphify (YC S26, MIT, Python ~50k LOC): tree-sitter knowledge graph of code+docs, LLM only for docs/media. NOT a competitor — it answers "how is code connected", DocGuard answers "do docs match code". Adoptable (all zero-dep-compatible): (1) **ADR/RFC citation check** — graphify's `extract.py:1087` regex `\b(ADR[- ]?\d{1,5}|RFC[- ]?\d{1,5})\b` in code comments, normalized to `ADR-0011`; DocGuard ships an ADR template but never verifies code citations point to existing ADR docs → natural REF-family extension (code→doc direction of reference-existence). (2) **`[[wikilink]]` support** — graphify's markdown extractor resolves wikilinks (extension-less → sibling .md) + strips `#anchor`/`?query` before resolution + `(?<!\!)` image guard; audit cross-reference/impact link parsing for these cases (Obsidian-style vaults). (3) **code→doc impact** — `affected.py` pattern: seed + BFS over a typed-edge whitelist; could extend `impact` beyond doc→doc using architecture.mjs import graph + trace links. (4) **Optional graph.json interop** — if `graphify-out/graph.json` exists, consume it (plain JSON read, zero-dep) as extra traceability evidence — but ONLY edges tagged `EXTRACTED` (its doc-side edges can be LLM-INFERRED; importing those breaks the determinism anchor). (5) **PreToolUse nudge hooks as distribution** — graphify's per-platform installs write hooks that fire before search/read tools; DocGuard analog: nudge `docguard impact` when the agent edits canonical docs. (6) Hygiene: git union-merge driver for committed generated JSON; relative-path re-anchored cache keys (audit plan.cache.json portability). Rejected: tree-sitter/NetworkX engine (Python dep stack vs zero-dep Node), LLM in core (already rejected), embeddings-free-graph marketing frame is theirs.

- **2026-07-07 (v0.31.0 accuracy release — 6 research-backed detectors, empirical keep/cut)** — Implemented the verified deep-research roadmap as one batched release (PR #311, 9 commits, validator count 24→27). Built on TWO zero-dep foundations reused across features: `cli/shared-diff.mjs` (unified-diff parser + identifier-aware tokenizer + activity-labeled diff) and `cli/shared-ir.mjs` (TF-IDF + cosine). Features: (1) `impact` doc→doc BLAST RADIUS incl. agent files — the flagship, unclaimed by competitors; (2) `reference-existence` REF001 two-revision check; (3) `diff-suspicion` DSP001 diff-overlap; (4) `api-doc-smells` APS001/2 Bloated/Lazy; (5) IR soft-link recovery folded into traceability TRC004; (6) `verify --since` structured-diff staging. **METHOD THE USER MANDATED (durable preference): don't pick validator defaults abstractly — build each, run it READ-ONLY against their real active repos (temp/no-writes), measure signal-vs-noise, KEEP/CUT/tune empirically, remove if no value.** Corpus: hugocross_revamp, taxes_helper, stablecoin-strategy-lab, Agent Ops Platform, globalshares_revamp (~/Repo_claude), Whatsapp_Inbox (~/GitHub). All 6 KEEP; all default-ON, all soft (`confidence:'low'`, never break CI). Precision levers that mattered: DSP001 needed path/module-only refs (drop basename) + generic/HTTP/CSS token filter + per-doc cap (globalshares route-inventory doc flooded 45→capped). Validation log lived in scratchpad/validation-log.md.
- **2026-07-07 (Do-Not-Repeat: adding a default-OFF validator SPLITS DocGuard's self-count)** — `metrics-consistency` counts guard results where `status !== 'skipped'` (excludes a config-disabled validator), but `canonical-sync` counts the full validatorMap (includes it). So a validator added default-OFF makes the two self-counters disagree by 1 (26 vs 27), and DocGuard's own guard flags CSY003 + MET001 forever. Resolution chosen: default the validator ON (it was zero-FP + soft anyway) so both counters agree — cleaner than special-casing the count logic. If a future validator MUST be default-off, reconcile the two counters first. Also: `canonical-sync` CSY003 false-positives on a validators-TABLE row whose number collides with the word "validators" in its description (row `| 24 | Surface-Sync | …validators… |` read as "24 validators") — reword such descriptions (used "checks"). And `metrics-consistency` MET001 can't tell HISTORICAL counts ("v0.22 had 24 validators" in a changelog row) from current claims — reword historical rows to drop the bare number. `docguard fix --write` will wrongly rewrite those historical counts.
- **2026-07-06 (Jules PR triage policy — established)** — How to handle the Google Jules bot's auto-PRs, applied to a batch of 5 (#299 #300 #305 #306 #307). Rule: judge each on merit against the persona's own evidence bar, adopt real fixes CLEANLY (re-apply in our own commit without Jules's auto-generated `.agent/skills` churn), close the rest with a specific technical reason so the bot/human learns the bar. Verdicts recorded: (a) **#307 hooks /dev/null fix = genuinely correct** (one-line getHooksDir guard + a reproducing test) → adopted cleanly in #308, closed as superseded. (b) **#300 "CRITICAL command injection" = FALSE positive** — every flagged `execSync` target is a static string literal (`which specify`, `git log --since=...`) with no user-input interpolation, so no injection surface; also on a stale base (setup.mjs isCliAvailable already removed in #296). Closed — but harvested the ONE real nugget (score.mjs `git log | wc -l` shell pipe → `execFileSync`, Windows-portable). (c) **#299 Bolt regex hoisting = no benchmark, negative-value** — `new RegExp()` runs once per file (O(files)), never inside the match loop, so the claimed "O(N·M) bottleneck" is fiction; hoisting to a shared stateful `g`-regex + manual `.lastIndex=0` is a footgun for zero measured gain. Closed per Bolt's own ">20% measured benchmark" rule. (d) **#305/#306 = +0/-0 status reports** ("babel up to date", "no flaky tests") — well-behaved (no spurious diff) but shouldn't be PRs; closed with thanks. Meta-lesson: the persona files (.jules/*.md) + AGENTS.md bot rules + the jules-triage.yml workflow set the bar; enforcing it consistently in close-comments is what actually trains the bot down from duplicate-PR floods. Two of five carried real value — Jules is worth keeping, not silencing.
- **2026-07-05 (distribution flywheel — v0.30.0)** — Executed the full external-distribution push. Durable facts: (1) **Official MCP Registry** publish needs THREE things aligned: `server.json` at repo root (2025-12-11 camelCase schema, name `io.github.raccioly/docguard`), `mcpName` in the PUBLISHED npm package (registry validates the referenced package carries it — so a release must SHIP after adding mcpName; adding it to main isn't enough), and `mcp-publisher login github && mcp-publisher publish` run FROM THE REPO ROLE on the user's Mac (I can't run it — sandbox ≠ their terminal; their GitHub device-auth token is local). Registry is IMMUTABLE per version → "400 duplicate version" means it ALREADY published (success, not failure). Verify via `curl https://registry.modelcontextprotocol.io/v0/servers?search=docguard`. (2) **Glama** listing: submitted via their web form (browser); the quality Score has code-fixable items — `glama.json` (root, `{$schema, maintainers:[gh-user]}`) fixes "No glama.json"; read-only MCP tool `annotations` (readOnlyHint/openWorldHint:false) + `title` serve the tool-def-quality/safety scans; a `Dockerfile` running `docguard mcp` enables their build-based "Glama release" (the big score driver, but the Make-Release clicks are the user's). (3) **awesome-mcp-servers** requires the server live on Glama first (bot label missing-glama→has-glama on re-check after a `synchronize`/force-push); entry format = `[owner/repo](url) [![badge](glama .../badges/score.svg)](glama url) 📇 🏠 🍎 🪟 🐧 - desc \`npx cmd\`` (badge AFTER repo link per file convention, NOT after description as the bot template says). (4) **Homebrew tap**: separate public repo `raccioly/homebrew-tap` with `Formula/docguard.rb`; formula url = npm tarball, sha256 must be recomputed per version (`curl -sL <tgz>|shasum -a256`). Bump the tap formula each release. (5) **spec-kit catalog**: one OPEN submission issue at a time (latest version), close superseded ones — established pattern. (6) **PATH shadowing gotcha**: a stale `npm i -g docguard-cli` shadows Homebrew's copy; `docguard --version` shows the FIRST-in-PATH one → `npm i -g docguard-cli@latest` to reconcile.


- **2026-07-03 (competitive scan, post-v0.29)** — Spec-kit catalog now 200+ extensions, ~45 doc/validation-adjacent; presentation is alphabetical with NO ranking — the description field + linked README are the only differentiation. Most entries are thin prompt-packs (SKILL.md workflows); DocGuard is one of few with a real engine (CLI, tests, npm/PyPI, MCP, SARIF, Action). Direct drift-competitors in-catalog: Spec Sync (bgervin), Reconcile (stn1slv), CI Guard (Quratulain-bilal), Coding Standards Drift Control, Golden Demo (behavioral drift), Retrospective (per-feature adherence scoring). IDEAS WORTH ADOPTING: (1) phantom-completion detection à la Verify Tasks ([X] in tasks.md with no implementation evidence — deterministic, fits spec-kit validator); (2) MemoryLint-style agent-instruction drift audit (contradictory/redundant rules WITHIN AGENTS.md/CLAUDE.md — fits extractor+LLM-judgment pattern); (3) per-feature spec-adherence scoring (Retrospective); (4) reviewer TLDR briefing (partially covered by Action pr-comment); (5) we already HAVE Mermaid ER generation (generateERDiagram) but never market it — surface in README. Outside competition unchanged: Mintlify llms.txt-at-scale and Swimm MCP matched by v0.29; hackathon-stage "DriftGuard" clones validate the direction. Catalog description should lead with MCP+SARIF+standalone engine — edit release.yml sync-catalog prefill for future releases; user can edit #286's description field in-form before submitting.

- **2026-07-03 (integration roadmap approved — "do it all")** — After the production-readiness batch, user asked "what's missing vs competition (Swimm/Mintlify/Context7/DeepWiki)?" Analysis: detection core is the moat ("green = docs verifiably match code"); the gap is INTEGRATION SURFACE — the tool assumes a human terminal or an agent that shells out. User approved all six: (1) **MCP server** `docguard mcp` — JSON-RPC 2.0 over stdio, pure Node, tools = guard/diagnose/explain/verify_claims/score, MUST be in READ_ONLY_COMMANDS (no ensureSkills side effects); (2) **SARIF 2.1.0** `guard --format sarif` — 1:1 from findings[] codes/locations → GitHub Code Scanning; (3) **Action PR annotations** — ::warning per finding + sticky doc-impact comment via diff --since; (4) **agent-file sync** `docguard agents --sync` — AGENTS.md canonical → CLAUDE.md/.cursor/rules/copilot-instructions/GEMINI.md generated variants with markers (drift-proofing the agent-file family); (5) **llms-full.txt + context pack** (`llms --full`, `memory --pack`); (6) **agent-readability score axis** — display-only (grades stay stable), deterministic metrics (token budget, anchor addressability, marker density). Explicitly REJECTED: LLM calls in core (trust anchor), hosted docs platform, auto-generated wikis. Execution: 6 parallel agents on disjoint files; README/AGENTS/CHANGELOG + docguard.mjs flag wiring reserved to orchestrator (same pattern as the 24-validator migration).

- **2026-06-02** — Triage-only pass on the websec-validator field report (no code changed, user's choice). Outcome that justified it: B1's reported root-cause was wrong (glob bug, not wiring), B4 is real-on-main via the dispatcher's `ensureSkills` (not the `--plan` code), B6 is partly-fixed/deferred-by-design. Full verified fix-list lives in the cross-session memory `websec-field-feedback-triage.md`.
- **2026-06-03** — F1 scope: **flag, don't suppress.** For scanner/tool false-positives, generate marks auto-extracted surface 'low' confidence for non-web kinds (cli/library/unknown) but never removes it from guard — suppressing a real surface would be a false-green, the failure mode this tool exists to prevent. User chose this over require-import (which risks false-negatives).
- **2026-06-03** — F3 scope: added `cli`/`library` **profiles** (non-web required sets) as the contained fix for the web-app-centric doc model. Deliberately did NOT build a bespoke `CLI-REFERENCE` doc *type* (template + documentTypes registry + TRACE_MAP + memory-plan + writer = 5+ files) — that's a separate larger feature. Until then `library` reuses API-REFERENCE as the module-API doc.
- **2026-06-03** — Pattern for non-trivial validator tests: provoke the deterministic always-emitted section (`tech-stack` in ARCHITECTURE, `memory-plan.mjs:243`) and assert with a **control** (unpinned→stale) + **fix** (pinned→exempt). Same control discipline used for B4 (.agent created by full generate, not by --plan) and F1 (cli→low, api→normal). Keeps tests non-vacuous.
- **2026-06-23** — Field report #6 (websec-validator): guard A+/"Accurate 100%" over a wrong "16 extractors". Its DIAGNOSIS was right (the Accurate pillar never fact-checked; the literal cause was a missing vocab noun) — but its PRESCRIPTION ("validate every claim in every doc, default-on, as warnings") was REJECTED as recall-maximizing. It re-introduces exactly the FP floods prior field reports forced us to scope away (wu-whatsappinbox ~39 FPs; Bug #2 subject-binding; changelog skipping; the explicit "precision over recall" header in semantic-claims.mjs). Chose the precision-first subset: (1) honesty fix to the `Accurate` label — display-only, gating grade untouched; (2) opt-in `config.collections` for deterministic count-drift; (3) non-gating coverage + unverified-claim VISIBILITY (surface what was NOT checked rather than deep-scan everything). Explicitly did NOT build universal prose-claim inference. Verified: headline grade unchanged (100/100 A+), ALCOA dropped 9/9→8/9 (honest), 5 new tests, no regressions (805/805 prior). Held release per user — accumulating into next version; full detail in CHANGELOG [Unreleased]. Reusable principle: a correct bug report can still carry an over-scoped fix — separate the diagnosis from the prescription.
- **2026-06-23 (follow-up)** — "Should a clearly-documentation folder be auto-tracked, and should we UPDATE a wrong doc or SUGGEST?" Decisions: (1) Auto-detect conventional doc-home NAMES (extend-not-replace via `config.docs.dirs`, exclude via `.docguardignore`) — answers "treat all clearly-doc folders as docs" without the arbitrary-subdir FP flood. (2) Remediation policy = DocGuard detects DIVERGENCE, not which side is right (the doc may be correct intent and the CODE regressed — the CDD premise). So: auto-fix ONLY the provably-mechanical class (counts bound to a collection) and ONLY via explicit `fix --write` (provenance, fail-closed, git-reversible, never silent); everything semantic/prose is SUGGEST-only (finding + `verify --semantic`), because the deterministic core can't author correct prose and must not assume code is the source of truth. Enrichment/rewriting is an agent task the human approves — NOT a validator silently editing docs. Recorded in CHANGELOG [Unreleased] "Remediation policy".
